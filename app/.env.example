# Copy to .env for local `netlify dev`, or set as a real environment
# variable in your Netlify site's dashboard (Site configuration -> Environment
# variables). Never commit your actual key.
ANTHROPIC_API_KEY=sk-ant-...
